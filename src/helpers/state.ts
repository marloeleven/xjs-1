// import {BehaviorSubject} from 'rxjs/BehaviorSubject';
import { BehaviorSubject } from 'rxjs';

import isSplitMode from './is-split-mode';

export const is = {
  splitMode: new BehaviorSubject(false),
};

(async function init() {
  // isSplitMode(xjs.internal).then();
})();
